/*import { contextBridge, ipcRenderer } from "electron";

const element = document.querySelector(".button_save") as HTMLButtonElement;

if (element === null) {
    // eslint-disable-next-line no-console, @typescript-eslint/restrict-plus-operands
    console.log("Can not find element with id " + element);
}

const child = document.createElement("DIV");

child.innerHTML = "hello World!";

child.style.margin = "20px";
child.style.textAlign = "center";

// eslint-disable-next-line @typescript-eslint/no-unsafe-call
element.appendChild(child);*/
